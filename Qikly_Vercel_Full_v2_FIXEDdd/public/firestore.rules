rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // The site reads public data through the server API.
    // Keep direct client writes disabled; Firebase Admin on the server handles all mutations.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
