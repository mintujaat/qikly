const $=s=>document.querySelector(s);const params=new URLSearchParams(location.search);const next=params.get("next")||"/";const incomingRef=(params.get("ref")||"").trim();let mode="login";
async function api(url,opt={}){const r=await fetch(url,{cache:"no-store",...opt,headers:{"Content-Type":"application/json",...(opt.headers||{})}});const d=await r.json().catch(()=>({}));if(!r.ok)throw Error(d.error||`Request failed (${r.status})`);return d}
function setMode(m){mode=m;document.querySelectorAll(".tab").forEach(b=>b.classList.toggle("active",b.dataset.mode===m));$("#nameWrap").classList.toggle("hidden",m!=="signup");$("#referralWrap").classList.toggle("hidden",m!=="signup");$("#name").required=m==="signup";$("#referralInput").value=incomingRef;$("#authTitle").textContent=m==="signup"?"Create your account":"Welcome back";$("#authSub").textContent=m==="signup"?"Set up your profile, wallet and personal referral link.":"Login to access your wallet and investment dashboard.";$("#submitBtn").textContent=m==="signup"?"Create Account →":"Login →";$("#authError").textContent="";$("#referralNotice").classList.toggle("hidden",!(m==="signup"&&incomingRef))}
document.querySelectorAll(".tab").forEach(b=>b.onclick=()=>setMode(b.dataset.mode));
if(incomingRef)setMode("signup");
(async()=>{try{const m=await api("/api/auth/me");if(m.authenticated)location.href=next}catch{}})();
$("#authForm").onsubmit=async e=>{e.preventDefault();$("#authError").textContent="";const btn=$("#submitBtn");btn.disabled=true;try{const body={email:$("#email").value.trim(),password:$("#password").value};if(mode==="signup"){body.name=$("#name").value.trim();body.referralCode=$("#referralInput").value.trim()}await api(mode==="signup"?"/api/auth/signup":"/api/auth/login",{method:"POST",body:JSON.stringify(body)});location.href=next}catch(err){$("#authError").textContent=err.message}finally{btn.disabled=false}};
